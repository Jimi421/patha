{
  "id": "cms_version_enum",
  "phase": "WEB",
  "title": "CMS Fingerprint + Version — Pin the Exact Build",
  "body": "You found a web app but it's not one with a dedicated node (WordPress/Grav/etc). Two jobs: identify WHICH CMS it is, then pin the EXACT version — because public exploits are version-specific and the version is what turns 'a CMS' into 'CVE-XXXX, run this'.\n\nAPP IN A SUBDIRECTORY: the root often looks empty while the real app lives under /test, /cms, /site, /blog. Your dir scan reveals it (a 301 to /test/ with the app inside). Don't conclude 'nothing here' from a bare root — follow the redirect.\n\nFINGERPRINT by signature paths/files: zp-core/ + zp-config.php = Zenphoto; wp-content/ = WordPress; /administrator + /components = Joomla; /user/login + /sites/default = Drupal; a 'generator' meta tag often names it outright.\n\nFIND THE VERSION (in order of reliability): view-source for a generator meta tag; a version.php / CHANGELOG / README / License file (CMSs ship these); the docs folder (Zenphoto leaves doc_files/*.pdf naming the release); the admin login footer. Directory listing enabled on the app's folders hands you all of this for free.\n\nOnce you have 'CMS X version Y', go hunt the CVE — that's the searchsploit step.",
  "cmd": "# ════════════════════════════════════════════════════════\n# 1. App in a subdir? Follow the redirect, map the CMS\n# ════════════════════════════════════════════════════════\ncurl -s http://$ip/ | grep -iE 'generator|powered by|<title'   # meta generator names the CMS\nwhatweb http://$ip/test/                                        # version guesses + tech stack\n# signature paths: zp-core (Zenphoto) | wp-content (WP) | administrator (Joomla) | sites/default (Drupal)\n\n# ════════════════════════════════════════════════════════\n# 2. Pin the exact version (try these in order)\n# ════════════════════════════════════════════════════════\ncurl -s http://$ip/test/ | grep -iE 'ver|version|generator'\ncurl -s http://$ip/test/zp-core/version.php\ncurl -s http://$ip/test/doc_files/README.html | grep -iE 'version|release'\ncurl -s http://$ip/test/CHANGELOG.txt | head\n# CMSs leave docs that name the release — Zenphoto: doc_files/*_quick_reference.pdf\n\n# ════════════════════════════════════════════════════════\n# 3. Directory listing = free loot (your dir scan flagged these)\n# ════════════════════════════════════════════════════════\ncurl -s http://$ip/test/zp-data/        # config dir — DB creds in zp-config.php (read on-box later)\ncurl -s http://$ip/test/uploaded/       # upload dir — file-upload-to-RCE candidate\n\n# ════════════════════════════════════════════════════════\n# 4. Fuzz for FILES now that you have the structure (extensions matter)\n# ════════════════════════════════════════════════════════\nferoxbuster -u http://$ip/test -w /usr/share/seclists/Discovery/Web-Content/raft-medium-files.txt \\\n  -x php,txt,bak,old,sql,zip,conf -s 200,301,302,403 -d 1\n\n# ── SUBRION CMS ──────────────────────────────────────────────\n# Fingerprint: meta generator tag, /panel/ admin login, footer says version\n# Admin panel: usually at /panel/ — check robots.txt Disallow entries first\n# Version: footer of /panel/ login page: \"Powered by Subrion CMS v4.2.1\"\ncurl -s http://hostname/panel/ | grep -iE 'subrion|powered by|version|generator'\n# CVE-2018-19422: authenticated file upload → RCE (4.2.1)\n# Try admin:admin before pulling the exploit — default creds work often\n# → see subrion node for full chain",
  "warn": "A 200 with 0 bytes (like zp-config.php over HTTP) means PHP executed and hid the source — you can't read it remotely, but the on-box file holds DB creds once you have a shell. Hunt config BACKUPS instead (zp-config.php.bak/.old/~) — those serve as plaintext. And keep the status filter on (-s 200,301,302,403) or a large wordlist drowns you in 404s from scraped CMS directory names.",
  "choices": [
    {
      "label": "Got CMS + version — hunt the CVE",
      "next": "searchsploit_web"
    },
    {
      "label": "Have an exploit file — run it correctly",
      "next": "searchsploit_workflow"
    },
    {
      "label": "Found a login page — try default/brute",
      "next": "login_page"
    },
    {
      "label": "Directory listing exposed an upload dir",
      "next": "file_upload"
    },
    {
      "label": "PDFKit / wkhtmltopdf — error reveals gem version",
      "next": "pdfkit_rce"
    },
    {
      "label": "SeaCMS — randomised admin slug, SQLi, LFI",
      "next": "seacms"
    },
    {
      "label": "Back to web triage",
      "next": "web_enum"
    },
    {
      "label": "Subrion CMS — CVE-2018-19422 file upload RCE",
      "next": "subrion"
    }
  ]
}
